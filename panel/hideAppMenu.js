var hideAppMenu = {
	enable(panel) {
		// if (panel.statusArea.appMenu) {
		panel.statusArea.appMenu.hide();
		// }
	},

	disable(panel) {
		// if (panel.statusArea.appMenu) {
		panel.statusArea.appMenu.show();
		// }
	},
};
